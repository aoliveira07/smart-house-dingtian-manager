# Exemplos fictícios

Os payloads ilustram o contrato MQTT; não são inventário nem configuração inicial. Não os publique no broker de uma residência. O gerenciador gera o próprio namespace com UUIDs persistidos.

O exemplo light foi corrigido para `schema: basic`, validado contra HA Core 2026.9.2. `state_on`/`state_off` pertencem somente ao exemplo switch.
